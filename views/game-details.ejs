<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= games.gameName %> | Details</title>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@600;700&family=Inter:wght@400;500&display=swap"
      rel="stylesheet"
    />

    <link rel="stylesheet" href="/style.css" />

  </head>
  <body>
    <%- include('navbar') %>

    <h1><%= games.gameName %></h1>

    <p><strong>Platform: </strong> <%= games.platform %></p>
    <p><strong>Genre: </strong><%= games.genre || "Not specified" %></p>
    <p><strong>Hours played: </strong> <%= games.hoursPlayed ?? 0 %></p>

    <p>
      <strong>Completed:</strong>
      <%= games.completed ? "Yes ✅" : "No ⏳" %>
    </p>

    <% if (games.rating) { %>
      <p><strong>Rating:</strong> <%= games.rating / 5 %> ⭐</p>
    <% } %>

    <hr>

    <a href="/games/<%= games._id%>/edit">Edit Game</a>

    <form action="/games/<%= games._id%>/delete" method="post" style="display: inline">
      <br>
      <button type="submit"> 🗑️ Delete Game</button>
      </form>

      <br>
      <a href="/games/allgames">Back to all games</a>


  </body>
</html>
